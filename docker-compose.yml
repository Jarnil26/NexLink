version: '3.8'

services:
  backend:
    build:
      context: ./ChatPlatform
      dockerfile: Dockerfile
    ports:
      - "5142:8080"
    environment:
      - ConnectionStrings__MongoDb=mongodb+srv://jarnilvisionaryiq_db_user:BCRAoERNHqh7GaEo@cluster0.uizpdm9.mongodb.net/chatplatform?appName=Cluster0
      - ConnectionStrings__Redis=redis:6379,abortConnect=false
      - Jwt__Key=SuperSecretKeyThatIsAtLeast32BytesLong!
      - Jwt__Issuer=ChatPlatformApi
      - Jwt__Audience=ChatPlatformClients
      - AllowedOrigins=http://localhost:80
    depends_on:
      - redis

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
